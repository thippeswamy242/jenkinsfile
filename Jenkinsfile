pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo "The build number is  ${BUILD_NUMBER}"
            }
        }
    }
}
